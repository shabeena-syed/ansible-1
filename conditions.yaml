- name: conditions
  become: yes  
  hosts: localhost
  tasks:
  - name: chek user exist rnot
    ansible.builtin.command: id expense
    ignore_errors: true
    
  - name: say hello
    ansible.builtin.debug:
     msg: "helo"